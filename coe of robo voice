import os

if __name__== '__main__':
    print("welcome to robo")
    while True:
        x=input("enter what you want to speack")
        if x=="q":
            os.system("say 'bye'")
            break
        command=f"say{x}"
        os.system(command)
